@Library('jenkins-ansible-shared-lib') _

pipeline {
    agent any
    stages {
        stage('Deploy') {
            steps {
                ansibleDeploy(configFile: 'deploy-config.yml')
            }
        }
    }
}
