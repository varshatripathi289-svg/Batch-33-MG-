@Library('jenkins-ansible-shared-lib') _

pipeline {
    agent any

    stages {
        stage('Run Ansible') {
            steps {
                ansibleRun(
                    INVENTORY: 'inventory',
                    PLAYBOOK: 'site.yml'
                )
            }
        }
    }
}
